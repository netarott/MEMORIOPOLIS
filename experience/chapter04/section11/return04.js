(()=>{'use strict';const sections=[...document.querySelectorAll('[data-return-section]')];if(!sections.length||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const observer=new IntersectionObserver(entries=>{const visible=entries.filter(entry=>entry.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!visible)return;const style=getComputedStyle(visible.target);document.documentElement.style.setProperty('--page-a',style.getPropertyValue('--tone-a'));document.documentElement.style.setProperty('--page-b',style.getPropertyValue('--tone-b'));},{threshold:[.25,.5,.75]});sections.forEach(section=>observer.observe(section));})();
